2026-03-10 1: Added Hono microservice scaffold

- Created `services/figma-microservice` with Bun + Hono entrypoint and type-safe TS setup.
- Added sitemap discovery endpoint + locale detection from URL paths.
- Added job pipeline endpoints for page x locale x breakpoint combinations.
- Added in-browser dev UI at `/dev` for discover/select/run/poll flow.
- Added root scripts (`microservice:dev`, `microservice:typecheck`) and docs.

2026-03-10 2: Docker + dev UI auth

- Added `services/figma-microservice/Dockerfile` and `.dockerignore` for Coolify deployment.
- Added `DEV_UI_BASIC_AUTH` support to protect `/dev` via HTTP Basic auth.
- Documented Docker/Coolify env setup in microservice README.

2026-03-10 3: Capture pipeline + artifact API

- Added Playwright-based screenshot capture for each page x locale x breakpoint task.
- Persists artifacts under `runs/<jobId>/...` and writes `manifest.json` for each job.
- Added artifact listing/download endpoints.
- Switched Docker base to Playwright image and installed Bun for containerized capture compatibility.

2026-03-10 4: Dev UI artifact filters

- Added locale and breakpoint filter controls in `/dev`.
- Added client-side artifact filtering with preserved thumbnail links.

2026-03-10 5: Figma write integration via dev resources

- Added real Figma write path using `POST /v1/dev_resources`.
- Added per-job support for `figmaNodeId` and `artifactPublicBaseUrl`.
- Added env-based write controls (`FIGMA_WRITE_MODE`, `FIGMA_TARGET_NODE_ID`, `ARTIFACT_PUBLIC_BASE_URL`).
- Updated `/dev` job form fields and docs for the new Figma write settings.

2026-03-10 6: Microservice env + gitignore

- Added `services/figma-microservice/.env` with runtime defaults/placeholders.
- Added `services/figma-microservice/.gitignore` for env files, runs artifacts, and local noise.

2026-03-10 7: Hreflang-based locale discovery

- Locale discovery now parses `xhtml:link hreflang` from sitemap XML.
- Keeps URL-prefix locale detection as fallback.
- Added optional `SITEMAP_DEFAULT_LOCALE` for unprefixed default-locale routes.

2026-03-10 8: Auto base URL for Coolify

- Added automatic artifact base URL resolution from `COOLIFY_FQDN`.
- Added request-origin fallback when no explicit base URL is provided.
- Documented resolution order in microservice README.

2026-03-10 9: Dev UI cleanup for Figma config

- Removed manual inputs for node id and artifact base URL from `/dev`.
- Added read-only config display values sourced from env/auto resolution.
- Added inline help for how to find Figma file key from file URL.

2026-03-10 10: Figma project/file picker

- Added API endpoints for listing team projects and project files.
- Updated `/dev` to use Team -> Project -> File selection instead of manual file key entry.
- Added `FIGMA_TEAM_ID` env support for picker defaults.
- Clarified that creating projects/folders is not available via current public REST endpoints.

2026-03-10 11: Auto node resolution

- Made `FIGMA_TARGET_NODE_ID` optional by auto-resolving a default node from selected file.
- Added per-file cache for resolved default node IDs.
- Updated `/dev` and docs to reflect automatic node behavior.

2026-03-10 12: Bun env source standardization

- Replaced `process.env` usage in microservice source with `Bun.env` via shared helpers.
- Ensures env reads are consistent with Bun runtime behavior across local/dev/container runs.

2026-03-10 13: Job monitor + reduced polling noise

- Added `/dev` job monitor with status, progress bar, and latest task summary.
- Reduced polling frequency and output churn while job is running.
- Added backend per-task logs and final summary logs in job runner.

2026-03-10 14: Throughput and resilience improvements

- Fixed dev resources payload to use required `dev_resources` array body.
- Added API timeout handling for Figma requests.
- Switched capture flow to reuse one browser per job and cache locale contexts.
- Added configurable concurrent task workers (`JOB_CONCURRENCY`) for faster jobs.

2026-03-10 15: Rate-limit and node-cap safeguards

- Added pre-check/caching for current dev-resource count per node and skip once max reached.
- Added retry/backoff handling for `429` responses on dev-resource writes.
- Reduced noisy hard-fail behavior when Figma node capacity is exhausted.

2026-03-10 16: Plugin importer connected to microservice jobs

- Rewrote `plugins/layoutlens-importer` to consume `/api/jobs` endpoints instead of legacy `/manifest`.
- Added plugin prompt UI for `baseUrl` + optional `jobId` (defaults to latest finished job).
- Implemented canvas import grouping by route + locale with breakpoint frames and image placement.

2026-03-10 17: Dev UI post-job handoff + roadmap

- Added post-job panel in `/dev` with job id, copy button, and "Open file in Figma" action.
- Added inline next-step instructions for plugin import handoff.
- Added `services/figma-microservice/AUTOMATION_PLAN.md` with phased execution plan.
